apiVersion: apps/v1
kind: Deployment
metadata:
  name: sprint-service
  namespace: enterprise-platform
spec:
  replicas: 1
  selector:
    matchLabels:
      app: sprint-service
  template:
    metadata:
      labels:
        app: sprint-service
    spec:
      containers:
        - name: sprint-service
          image: ghcr.io/enterprise-platform/sprint-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8086
          envFrom:
            - configMapRef:
                name: platform-config
          env:
            - name: SPRING_DATASOURCE_URL
              value: "jdbc:postgresql://postgres.enterprise-platform.svc.cluster.local:5432/sprint_db"
            - name: SPRING_DATASOURCE_USERNAME
              valueFrom:
                secretKeyRef:
                  name: platform-secrets
                  key: POSTGRES_USER
            - name: SPRING_DATASOURCE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: platform-secrets
                  key: POSTGRES_PASSWORD
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "512Mi"
              cpu: "1000m"
---
apiVersion: v1
kind: Service
metadata:
  name: sprint-service
  namespace: enterprise-platform
spec:
  selector:
    app: sprint-service
  ports:
    - port: 8086
      targetPort: 8086
